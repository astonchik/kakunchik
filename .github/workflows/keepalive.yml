name: keepalive-render

on:
  schedule:
    - cron: "*/14 * * * *"   # каждые 14 минут
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render service
        run: curl -sS https://kakunchik.onrender.com/ > /dev/null
